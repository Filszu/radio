import { Button } from "./ui/button"

type Props = {}

const AdvancedMusicList = (props: Props) => {
  return (
    <>
    <Button className="w-10 mx-1"  onClick={()=>{}}>
            Pokaż więcej
        </Button>
   </>
  )
}

export default AdvancedMusicList